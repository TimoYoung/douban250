# CONTEXT.md

## 术语表

### 爬虫任务
指完整的爬取流程，如"爬取豆瓣Top 250"或"爬取IMDb Top 250"。与单个HTTP请求不同，一个爬虫任务包含多个步骤：获取页面、解析数据、保存到数据库、创建版本等。

### 重试机制
当爬虫任务失败时，系统自动安排后续重试的机制。与HTTP请求层的重试（指数退避、最多3次）不同，这是任务级的重试，间隔更长（1小时），用于处理网络中断、反爬封锁等导致的整个任务失败。

### 重试管理器
新建的模块，统一管理所有爬虫的重试逻辑。负责：
- 跟踪重试状态（次数、时间、等待中/运行中/已取消/已耗尽）
- 安排重试（使用APScheduler的`DateTrigger`）
- 处理取消请求
- 与定时任务协调

### 重试状态
爬虫任务的重试状态，包括：
- `pending`：等待重试
- `running`：正在重试
- `cancelled`：已取消
- `exhausted`：已耗尽所有重试次数
- `failed`：最终失败（重试后仍失败）

### 重试参数
可配置的重试参数，存储在数据库`settings`表中：
- `retry_interval`：重试间隔（默认3600秒，即1小时）
- `max_retries`：最大重试次数（默认3次）

### 取消机制
取消等待中的重试。有两种触发方式：
1. 用户通过API手动取消
2. 用户手动触发爬虫时自动取消该爬虫的等待重试

### 进度端点
前端查询爬虫进度的API端点。扩展现有端点以包含重试状态信息：
- `GET /api/crawl/progress`：豆瓣爬虫进度
- `GET /api/crawl/imdb/progress`：IMDb爬虫进度

### CrawlLog
数据库表，记录每次爬虫运行的日志。每次重试都创建独立的CrawlLog记录，便于追踪每次尝试。

### 定时任务
APScheduler调度的定期任务。当定时任务触发时，如果有等待中的重试，取消重试并运行定时任务。

### 重试配置
控制重试行为的参数，包括重试间隔和最大重试次数。这些参数存储在数据库settings表中，可通过API修改。前端提供配置界面，允许管理员自定义这些参数。

### cron下次运行时间
根据cron表达式计算的下次定时任务执行时间。前端显示此时间，帮助用户了解下次自动执行的时间点。
