用python+vue3，前后端分离B/S架构，实现一个豆瓣top250电影记录和分析的系统
部署方式要支持docker前后端一体打包。
python开发虚拟环境用uv以及pyproject.toml管理
支持cron定时或手动触发爬取最新的top250电影列表和电影的元数据信息（图片只需保存一张海报），存入数据库。（默认sqlite,也要支持配置外部mysql或postgres数据库）
https://movie.douban.com/top250
每次爬取的电影列表要是和上一期不同，则以当次爬取日期为tag创建一个新的top250版本，若列表相同，则不创建新的版本
支持配置豆瓣用户id,要能读取用户的“看过”标签，和top250电影作对比

https://movie.douban.com/people/166675383/collect
前端要支持：
- 配置cron表达式和豆瓣用户id，还有手动更新按钮
- 按版本显示top250电影列表，可以切换版本。展示方式默认是带分页的列表，可以按看过筛选。也要支持紧凑的电影“气泡”，在一屏页面显示所有电影，看过的和未看过的是不同颜色的气泡。
- 每一部电影的详情页面，除了显示保存的元数据，还要能展示在历史版本中的排序，按顺序用图表展示
- 每个版本（除第一个版本），要展示变化的摘要。要包括相比上一个版本新增了哪些电影，删除了哪些电影，排名上升和下降最大的n部电影。这个比较功能要放在按版本显示的电影列表页面。比较对象默认是上个版本，也可以指定不同的版本。
